DROP SCHEMA IF EXISTS `database`;
CREATE SCHEMA IF NOT EXISTS `database` DEFAULT CHARACTER SET utf8;
USE `database`;

CREATE TABLE IF NOT EXISTS `database`.`whisper` (
    `channelid` VARCHAR(45) NOT NULL,
    `userid1` VARCHAR(45) NOT NULL,
    `userid2` VARCHAR(45) NOT NULL,
    PRIMARY KEY (`channelid`))